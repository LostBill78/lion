
use crate::Terminal::*;
use super::super::buffers::*;


pub struct Create {
    obj_type: &str,
    obj_name: &str,
    columns: Vec<Column>,
}


impl Create {
    pub fn define_create(dictionary: &Dictionary) -> Result<Table> {

    }
}

